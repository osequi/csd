import React from "react";
import ColorSpaces from "./ColorSpaces";

export default {
  component: ColorSpaces,
  title: "ColorSpaces",
};

const Template = (args) => <ColorSpaces {...args} />;

export const Default = Template.bind({});
Default.args = {};
