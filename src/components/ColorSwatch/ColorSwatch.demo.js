import React from "react";

/**
 * Imports other components and hooks.
 */
import ColorSwatch from ".";

/**
 * Displays the ColorSwatch demo.
 */
const ColorSwatchDemo = (props) => {
  return <ColorSwatch />;
};

export default ColorSwatchDemo;
