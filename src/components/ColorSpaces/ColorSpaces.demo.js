import React from "react";

/**
 * Imports other components and hooks.
 */
import ColorSpaces from ".";

/**
 * Displays the ColorSpaces demo.
 */
const ColorSpacesDemo = (props) => {
  return <ColorSpaces />;
};

export default ColorSpacesDemo;
