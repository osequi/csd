import React from "react";

/**
 * Imports other components and hooks.
 */
import CSD from ".";

/**
 * Displays the CSD demo.
 */
const CSDDemo = (props) => {
  return <CSD />;
};

export default CSDDemo;
