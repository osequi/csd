import React from "react";

/**
 * Imports other components and hooks.
 */
import Hues from ".";

/**
 * Displays the Hues demo.
 */
const HuesDemo = (props) => {
  return <Hues />;
};

export default HuesDemo;
