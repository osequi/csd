import React from "react";

/**
 * Imports other components and hooks.
 */
import ColorWheel from ".";

/**
 * Displays the ColorWheel demo.
 */
const ColorWheelDemo = (props) => {
  return <ColorWheel />;
};

export default ColorWheelDemo;
